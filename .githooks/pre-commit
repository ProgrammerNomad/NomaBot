#!/usr/bin/env python3
import subprocess
import sys
from pathlib import Path

ROOT = Path(__file__).resolve().parents[1]
result = subprocess.run(
    [sys.executable, str(ROOT / "scripts" / "check_secrets.py"), *sys.argv[1:]],
    cwd=ROOT,
)
raise SystemExit(result.returncode)
